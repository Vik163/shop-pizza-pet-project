{
  "presets": [
    "@babel/preset-env",
    "@babel/preset-typescript",
    [
      "@babel/preset-react",
      {
        // подставляется react без импортов
        "runtime": "automatic"
      }
    ]
  ]
}
