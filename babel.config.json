{
   "presets": [
      [
         "@babel/preset-react",
         {
            // подставляется react без импортов
            "runtime": "automatic"
         }
      ],
      ["@babel/preset-env", { "targets": { "node": "current" } }],
      "@babel/preset-typescript"
   ]
}
